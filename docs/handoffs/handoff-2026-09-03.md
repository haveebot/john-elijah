# Handoff — 2026-09-03 (build day)

**State:** built, deployed, verified. Production alias https://john-elijah.vercel.app (custom
domains attached, DNS pending at Bluehost). Deployment protection OFF for review.

## Verified live
- Every public route 200; homepage hero from Blob; 51 photos; residency + record + rate card render.
- `/api/book` E2E on prod → booking row + activity trail (mail no-op logged). Test row deleted.
- HQ login + all 8 surfaces 200 behind the session; unauth `/hq` → 307 `/hq/login`.
- Operator: `winston@johnelijahmusic.com` (password handed to Winston in chat). SIGNUP_KEY on Vercel env.

## Winston's tasks (gated on him)
1. Bluehost DNS: `johnelijahmusic.com` + `johnelijahband.com` — `@` A → 76.76.21.21, `www` CNAME → cname.vercel-dns.com. (Then flip `robots` in `src/app/layout.tsx`.)
2. Mailbox decision (admin@ / booking@ / contact@) + creation → Resend domain verify → `RESEND_API_KEY`, `MAIL_FROM`, `BOOKING_NOTIFY_EMAIL` on Vercel.
3. Stripe account (John's or ours) → `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`; Shippo signup → `SHIPPO_API_KEY`, `SHIP_FROM_JSON`.
4. Collie: the original John Elijah logo → replaces the wordmark + OG.
5. Photographer's name for the credit line (HQ → Photos, or `--credit` on re-import).
6. Confirm the rate card numbers (Settings → Rate card) and John's review of the bio.

## Not done / deliberately out
- 7/6 and 7/27 Drive sets are other bands — not imported.
- No motion ident yet (graphics pipeline is the path if wanted).
- No AI drafting wired (ANTHROPIC_API_KEY unused in v1).
